# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Go workspace files
go.work

# Dependency directories
vendor/

# Build directories
bin/
tmp/
dist/

# Go cache directories (Docker dev environment)
.go-bin/
.go-build-cache/
.go-mod-cache/

# Air temporary files
*.log

# JWT key mountpoint — the directory is TRACKED (see keys/.gitkeep) because it
# is the host-side target of the nested `./keys:/app/keys` bind mount; anything
# that may land in it is not.
keys/*
!keys/.gitkeep

# Environment files
.env
.env.local
.env.*.local

# IDE files
.idea/
*.swp
*.swo
*~
.vscode/
*.iml

# OS files
.DS_Store
Thumbs.db

# Debug files
debug

# Test coverage
coverage.html
coverage.out

# Logs
logs/
*.log
