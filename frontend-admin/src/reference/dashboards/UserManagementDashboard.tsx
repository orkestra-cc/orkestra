import UserManagementPage from 'pages/admin/users';

// Showcase mirror of the production /admin/users page — render the page
// itself so the reference stays in lockstep with the shipped header genre.
const UserManagementDashboard: React.FC = () => <UserManagementPage />;

export default UserManagementDashboard;
